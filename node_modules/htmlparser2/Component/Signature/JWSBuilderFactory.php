                       } else {
                                        emptyView.setVisibility(View.VISIBLE);
                                    }
                                }
                            }
                        });
                        break;
                    case MODIFIED:
                        if (myNotificationIdList.contains(dc.getDocument().getId())) {
                            int n = myNotificationIdList.indexOf(dc.getDocument().getId());
                            myNotificationList.remove(n);
                            myNotificationIdList.remove(n);
                            notificationAdapter.notifyDataSetChanged();

                            notifications mynoti1 = dc.getDocument().toObject(notifications.class);
                            mynoti1.setOrder(dc.getNewIndex());
                            FirebaseFirestore.getInstance().collection("users").document(mynoti1.getFrom()).get().addOnSuccessListener(documentSnapshot -> {
                                if (documentSnapshot.exists()) {
                                    Users m = documentSnapshot.toObject(Users.class);
                                    if (m.getIsActive()) {
                                        mynoti1.setUser(m);
                                        myNotificationIdList.add(dc.getDocument().getId());
                                        myNotificationList.add(mynoti1);
//      